import React from 'react';
import RequestTask from '@/components/task/requestTask'; // Adjust the path as per your project structure

const TaskRequestPage: React.FC = () => {
    return (
        <div>
            <RequestTask />
        </div>
    );
};

export default TaskRequestPage;