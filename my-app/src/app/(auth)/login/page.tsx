'use client';

import LoginForm from '@/components/login/loginForm';

const LoginPage: React.FC = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
