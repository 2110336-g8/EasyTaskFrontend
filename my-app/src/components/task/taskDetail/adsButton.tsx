'use client';

import { ViewTaskProps } from '@/types/task';
import { Avatar, AvatarFallback, AvatarImage } from '../../ui/avatar';

export default function AdsButtons(props: ViewTaskProps) {
    return <div className='flex flex-col gap-[16px]'></div>;
}
